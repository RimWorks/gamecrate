{
  "name": "gamecrate-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "typecheck": "npm run typecheck --workspaces",
    "test": "vitest run",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "oxlint": "1.83.0",
    "vitest": "^5.0.1"
  }
}
